

  {{#if blogs.length}}
  <div class="current-blogs">
    <h3>Your blogs:</h3>
    <ol class="blog-list">
    {{#each blogs as |blog|}}
    

   </ol>
    {{/each}}
  </div>
  {{/if}}
</div>

{{!--adding post info--}}
<article class="blog">
  <div class="blog-head">  
    <div class="blog-title">
      <a href="/dashboard/edit/{{id}}">{{blog.title}}</a>
    </div>    
</article>

<script src="./js/dashboard.js"></script>
